{
  "name": "build-and-tag-action",
  "version": "1.0.0",
  "private": true,
  "description": "Properly tags your GitHub Action after building",
  "main": "dist/index.mjs",
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "test": "vitest run",
    "build": "ncc build -o dist --minify src/index.ts && node --input-type=module -e \"import fs from 'fs'; fs.renameSync('dist/index.js','dist/index.mjs'); fs.rmSync('dist/package.json',{force:true});\"",
    "deps": "pnpm dlx npm-check-updates --target newest -u"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "semver": "^7.8.5",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/semver": "^7.7.1",
    "@vercel/ncc": "^0.44.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
